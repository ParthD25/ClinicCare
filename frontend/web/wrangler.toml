name = "cliniccare-web"
compatibility_date = "2024-01-01"

[build]
command = "npm run build"
cwd = "."

[build.upload]
dir = "out"
format = "service-worker"

[vars]
NEXT_PUBLIC_API_URL = "https://your-fastapi-backend-url.com"